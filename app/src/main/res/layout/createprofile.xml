<!-- res/layout/activity_create_profile.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FDFDFD"
    android:paddingStart="12dp"
    android:paddingEnd="12dp">

    <!-- Hamburger Menu on the left -->
    <ImageView
        android:id="@+id/hamburgerMenu"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:src="@drawable/hamburger" />

    <TextView
        android:id="@+id/createProfileText"
        android:layout_width="wrap_content"
        android:layout_height="28dp"
        android:layout_alignParentTop="true"
        android:layout_centerInParent="true"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:text="Create your profile"
        android:textColor="#1B1B1B"
        android:textSize="24sp" />

    <!-- Settings Icon on the right -->
    <ImageView
        android:id="@+id/settingsIcon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:src="@drawable/settings" />

    <!-- Text "Create your profile" in the center -->

    <!-- Image in the center with "add image" button -->
    <ImageView
        android:id="@+id/profileImage"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:layout_below="@id/createProfileText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:scaleType="centerCrop"
        android:src="@drawable/user" />

    <EditText
        android:id="@+id/nameText"
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/profileImage"
        android:layout_marginTop="20dp"
        android:background="#D5D5D5"
        android:hint="Your name"
        android:paddingStart="20dp"
        android:paddingEnd="12dp"
        android:textColor="#000000"
        android:textColorHint="#8C8C8C"
        android:textSize="36sp" />

    <!-- Text box for username -->
    <EditText
        android:id="@+id/quoteText"
        android:layout_width="360dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/nameText"
        android:layout_marginTop="16dp"
        android:background="#D5D5D5"
        android:hint="Add your quote"
        android:paddingStart="20dp"
        android:paddingEnd="12dp"
        android:textColor="#000000"
        android:textColorHint="#8C8C8C"
        android:textSize="32sp" />


    <!-- Big round button with plus sign -->

    <ImageView
        android:id="@+id/addContentImage"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_below="@id/quoteText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="60dp"
        android:src="@drawable/add" />

    <!-- Text "Add content" below the round button -->
    <TextView
        android:id="@+id/addContentText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/addContentImage"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:text="Add content"
        android:textColor="#000000"
        android:textSize="24sp" />



    <!-- Button "See guide prompts" in the middle -->


</RelativeLayout>
